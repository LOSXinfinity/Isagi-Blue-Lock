# Isagi-Blue-Lock
An interactive HTML5 canvas experience featuring Blue Lock's Isagi Yoichi. Moving the cursor reveals an "awakened" image (Direct/Ego mode) over a base image through a dynamic circular mask with trailing animation. Pure vanilla JS/CSS/HTML — zero dependencies.
